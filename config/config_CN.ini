[scan]
# 要扫描的目标目录，支持环境变量（如 %USERPROFILE%）
directory = %USERPROFILE%\Desktop\

# 文件匹配模式，多个模式用英文逗号分隔
patterns = *.ppt, *.pptx

# 是否递归扫描子目录：true 或 false
recursive = false

[time]
# 创建时间阈值（天数）：设 0 表示不限制创建时间
creation_days = 3

# 最后访问时间阈值（天数）：设 0 表示不限制访问时间
# 满足条件的文件将被移动至备份目录（而非直接删除）
access_days = 2
